<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:pages="clr-namespace:SubjectsManager.Pages"
             xmlns:models="clr-namespace:SubjectsManager.UIModels;assembly=SubjectsManager.UIModels"
             x:Class="SubjectsManager.Pages.SubjectsPage"             
             x:DataType="pages:SubjectsPage"
             Title="Subjects">
    <CollectionView
        ItemsSource="{Binding Path=Subjects}"
        Margin="10"
        SelectionMode="Single"
        SelectionChanged="SubjectSelected">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:SubjectUIModel">
                <Border
                    Stroke="Magenta"
                    Padding="10"
                    Margin="0,10">      
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup Name="CommonStates">
                            <VisualState Name="Normal" />
                            <VisualState Name="Selected">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="LightPink" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>

                    <VerticalStackLayout>
                        <Label
                            Text="{Binding Name}"
                            Style="{StaticResource SubHeadline}" />
                        <Label 
                            Text="{Binding KnowledgeArea, Converter={StaticResource EnumToDisplayName}}"
                            HorizontalOptions="Center" />   
                    </VerticalStackLayout>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>